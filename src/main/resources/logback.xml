<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File appender for animation debugging -->
    <appender name="ANIMATION_FILE" class="ch.qos.logback.core.FileAppender">
        <file>animation_debug.log</file>
        <append>false</append>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger - captures everything -->
    <root level="INFO">
        <appender-ref ref="STDOUT" />
        <appender-ref ref="ANIMATION_FILE" />
    </root>
</configuration>