<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender. Denied entirely in TUI mode (issue #1215): stdout there is the terminal surface
         TerminalRenderSurface owns exclusively, and an ordinary console log line racing its ANSI writes corrupts
         the display and drags the real cursor away, see TuiConsoleLogFilter. -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="com.serenity.diagnostics.TuiConsoleLogFilter" />
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Rolling app log for packaged desktop runs and crash diagnostics -->
    <appender name="APP_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${user.home}/.serenity/serenity.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${user.home}/.serenity/serenity.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>5MB</maxFileSize>
            <maxHistory>14</maxHistory>
            <totalSizeCap>50MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{48} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger - captures everything -->
    <root level="INFO">
        <appender-ref ref="STDOUT" />
        <appender-ref ref="APP_FILE" />
    </root>
</configuration>
